# 1. Update the package list

```bash
sudo apt update
```

# 2. Install Git

```bash
sudo apt install git -y
```

# 3. Verify the installation

```bash
git --version
```

You should see output similar to:

```text
git version 2.43.0
```

(The version number may differ depending on your Ubuntu release.)